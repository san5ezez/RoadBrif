'use client';import {LineChart,Line,AreaChart,Area,XAxis,YAxis,Tooltip,ResponsiveContainer,BarChart,Bar} from 'recharts';import {demoPlan} from '@/lib/demo';
const data=demoPlan.legs.map(l=>({name:l.to,km:l.km,temp:l.temp,wind:l.windSpeed,gust:l.gust,precip:l.precip,road:l.roadTemp,elev:l.elev,fuel:l.fuel}));
export function WeatherCharts(){return <div className="grid md:grid-cols-2 gap-4">{[['AIR TEMPERATURE','temp','#34d3ff'],['WIND SPEED','wind','#8cff9b'],['WIND GUSTS','gust','#f6b73c'],['PRECIPITATION','precip','#7aa7ff'],['ROAD TEMPERATURE','road','#ff6b4a']].map(([t,k,c])=><div className="tech p-3" key={k}><div className="label mb-2">{t}</div><ResponsiveContainer height={160}><LineChart data={data}><XAxis dataKey="name" hide/><YAxis/><Tooltip contentStyle={{background:'#07111f',border:'1px solid #1f3348'}}/><Line type="monotone" dataKey={k} stroke={c} strokeWidth={2}/></LineChart></ResponsiveContainer></div>)}</div>}
export function FuelChart(){return <ResponsiveContainer height={190}><BarChart data={data}><XAxis dataKey="name"/><YAxis/><Tooltip contentStyle={{background:'#07111f',border:'1px solid #1f3348'}}/><Bar dataKey="fuel" fill="#34d3ff"/></BarChart></ResponsiveContainer>}
export function ElevationChart(){return <ResponsiveContainer height={220}><AreaChart data={data}><XAxis dataKey="name"/><YAxis/><Tooltip contentStyle={{background:'#07111f',border:'1px solid #1f3348'}}/><Area dataKey="elev" stroke="#8cff9b" fill="#8cff9b33"/></AreaChart></ResponsiveContainer>}
