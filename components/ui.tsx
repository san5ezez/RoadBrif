import Link from 'next/link';import {ReactNode} from 'react';import {clsx} from 'clsx';
export function Button({children,className='',...p}:React.ButtonHTMLAttributes<HTMLButtonElement>){return <button className={clsx('px-4 py-2 border border-cyan/40 bg-cyan/10 text-cyan text-xs font-bold tracking-widest hover:bg-cyan/20 transition',className)} {...p}>{children}</button>}
export function Card({children,className=''}:{children:ReactNode;className?:string}){return <section className={clsx('tech p-4',className)}>{children}</section>}
export function Sidebar(){const items=['Dashboard','Create Plan','My Plans','Vehicles','Weather','Fuel','Route','Plan Viewer','Settings','About'];const href=(x:string)=>'/' + x.toLowerCase().replaceAll(' ','-');return <aside className="hidden lg:flex w-64 shrink-0 flex-col border-r border-line bg-black/40 min-h-screen p-5"><Link href="/" className="text-cyan font-black tracking-[.25em] mb-8">ROADBRIEF</Link>{items.map(i=><Link className="py-3 border-b border-line/60 text-sm tracking-widest text-slate-300 hover:text-cyan" href={href(i)} key={i}>{i}</Link>)}</aside>}
export function Shell({children}:{children:ReactNode}){return <main className="min-h-screen grid-bg flex"><Sidebar/><div className="flex-1 p-4 lg:p-8 overflow-hidden">{children}</div></main>}
export function Field({label,children}:{label:string;children:ReactNode}){return <label className="block"><div className="label mb-2">{label}</div>{children}</label>}
export const input='w-full bg-black/40 border border-line px-3 py-2 text-sm text-white outline-none focus:border-cyan';
